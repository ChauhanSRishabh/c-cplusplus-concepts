#include <stdio.h>

int main()
{

    printf("wow what a scene \n");
    int i = 0, j = 0;

    // FOR LOOP
    for (/* i=0,j=0 */i=0;i < 5; i++, j++) // expression 1, i=0 is not needed if i is already initialized
    {
        printf("%d %d \n", i, j);   
    }

    return 0;
}
